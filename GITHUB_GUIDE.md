# دليل بناء المثبّت عبر GitHub (خطوة بخطوة)

الهدف: تحصل على ملف **`SecureBatchPrinter-Setup.exe`** (مثبّت جاهز) دون تثبيت أي شيء على جهازك.
GitHub يبنيه لك في السحابة، وأنت فقط ترفع الملفات وتنزّل الناتج.

> تذكير: التطبيق الناتج **يعمل بلا إنترنت** على جهاز كل شخص. GitHub يُستخدم للبناء والتنزيل فقط.

---

## 1) أنشئ حسابًا على GitHub
- افتح <https://github.com> ← **Sign up** ← أدخل بريدك وكلمة مرور واسم مستخدم ← فعّل البريد.

## 2) أنشئ مستودعًا (Repository)
- بعد الدخول اضغط علامة **＋** أعلى اليمين ← **New repository**.
- الاسم: مثلاً `batch-printer`.
- اختر **Private** (خاص بك) أو **Public**.
- فعّل **Add a README file**.
- اضغط **Create repository**.

## 3) ارفع ملفات التطبيق
- داخل المستودع: **Add file ▾ ← Upload files**.
- من هذه الحزمة (بعد فكّ ضغطها) اسحب هذه الملفات الأربعة إلى الصفحة:
  `app.py` ، `ui.html` ، `requirements.txt` ، `installer.iss`
- انزل للأسفل واضغط **Commit changes**.

## 4) أضف ملف البناء (workflow)
- اضغط **Add file ▾ ← Create new file**.
- في خانة اسم الملف بالأعلى اكتب بالضبط:
  ```
  .github/workflows/build.yml
  ```
  (اكتب الشرطتين `/` وستُنشأ المجلدات تلقائيًا)
- افتح الملف `build.yml` الموجود في الحزمة (مجلد `.github/workflows/`)، انسخ **كل** محتواه، والصقه في المربّع الكبير.
- اضغط **Commit changes**.

## 5) انتظر البناء
- افتح تبويب **Actions** أعلى الصفحة.
- سترى عملية **Build Windows Installer** تعمل (دائرة صفراء). انتظر حتى تتحوّل إلى **علامة صح خضراء** (٣–٦ دقائق تقريبًا).
- إن لم تبدأ تلقائيًا: افتح **Actions** ← اختر **Build Windows Installer** يسارًا ← **Run workflow**.

## 6) نزّل المثبّت
- اضغط على العملية المكتملة (الخضراء).
- انزل لأسفل إلى قسم **Artifacts**.
- نزّل **`SecureBatchPrinter-Setup`** (سيأتي كملف مضغوط) ← فكّ ضغطه ← بداخله **`SecureBatchPrinter-Setup.exe`**.
- (يوجد أيضًا **`SecureBatchPrinter-portable-exe`** إن أردت نسخة ملف واحد تعمل بلا تثبيت.)

## 7) ثبّت وأرسل
- شغّل **`SecureBatchPrinter-Setup.exe`** ← ثبّته مثل أي برنامج (يضيف اختصارًا في قائمة ابدأ وسطح المكتب).
- أرسل ملف الـ Setup لأي شخص؛ يثبّته عنده مباشرة **بلا حاجة لبايثون ولا أي شيء**.

---

## إذا ظهرت علامة خطأ حمراء ❌
افتح العملية الفاشلة، اضغط على الخطوة الحمراء، **انسخ رسالة الخطأ وأرسلها لي** وأصلح ملف البناء فورًا. هذا طبيعي في أول مرة أحيانًا ولا يستغرق سوى تعديل بسيط.

## هل تريد رابط تنزيل دائم للناس؟
أقدر أضيف خطوة **Release** تلقائية تعطيك رابطًا ثابتًا للمثبّت تشاركه مباشرة. قل لي وأجهّزها.
